# service.yaml (versão modificada)
apiVersion: v1
kind: Service
metadata:
  name: safebank-webapp-service
spec:
  type: NodePort # MUDANÇA AQUI
  selector:
    app: safebank-webapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80